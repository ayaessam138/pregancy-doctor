import 'package:flutter/material.dart';

class appointments extends StatelessWidget {
  static String appointmentssid = 'appointments';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('appointments'),
    );
  }
}
