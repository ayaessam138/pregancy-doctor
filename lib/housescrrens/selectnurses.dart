import 'package:flutter/material.dart';

class selectnurses extends StatelessWidget {
  static String selectnursesid = 'selectnurses';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('selectnurses'),
    );
  }
}
